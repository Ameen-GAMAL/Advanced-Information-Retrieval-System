# Lightweight dependencies for the offline demo, unit tests and linting.
# These let you run the fusion / optimisation / evaluation core (and the
# synthetic demo) without the multi-gigabyte neural frameworks.
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10
pyyaml>=6.0
pytest>=7.0
